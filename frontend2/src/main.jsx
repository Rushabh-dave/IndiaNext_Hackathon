import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import { BrowserRouter } from 'react-router-dom'
import './index.css'
import App from './App.jsx'
import { setupHealthCheck, logConnectionDiagnostics } from './services/healthCheck.js'

// Initialize connection diagnostics and health check
logConnectionDiagnostics();
setupHealthCheck();

createRoot(document.getElementById('root')).render(
  <StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </StrictMode>,
)
